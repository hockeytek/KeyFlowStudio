# Core Qt framework
PySide6>=6.2.0

# Deep learning (with platform-specific markers for compatibility)
torch==2.2.2 ; sys_platform == 'darwin' and platform_machine == 'x86_64'
torchvision==0.17.2 ; sys_platform == 'darwin' and platform_machine == 'x86_64'
torch==2.8.0 ; sys_platform != 'darwin' or platform_machine != 'x86_64'
torchvision==0.23.0 ; sys_platform != 'darwin' or platform_machine != 'x86_64'

# Image and video processing
opencv-python==4.11.0.86
Pillow>=9.5.0
imageio>=2.25.0
imageio-ffmpeg>=0.4.8

# Utilities
numpy<2 ; sys_platform == 'darwin' and platform_machine == 'x86_64'
numpy ; sys_platform != 'darwin' or platform_machine != 'x86_64'
scipy>=1.7.0
tqdm>=4.66.0

# CorridorKey dependencies (model transformers, SAM-like architecture)
timm==1.0.24
transformers<5 ; sys_platform == 'darwin' and platform_machine == 'x86_64'
transformers ; sys_platform != 'darwin' or platform_machine != 'x86_64'
kornia>=0.6.0
huggingface-hub>=0.16.0
# GVM dependencies
easydict>=1.10
diffusers>=0.30,<0.32   # 0.32+ uses torch.xpu which requires torch>=2.4
peft>=0.10
accelerate>=0.26
matplotlib>=3.7
av>=9.0
pims>=0.6

# HTTP and system utilities
requests>=2.28.0
psutil>=5.9.0
boto3>=1.34  # AWS SDK — cloud GPU management

# For MatAnyone2 (if installing from source)
# Uncomment and update path as needed:
# -e /path/to/MatAnyone2
